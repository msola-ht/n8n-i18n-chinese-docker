name: 构建 n8n Task Runners Docker 镜像

on:
  workflow_dispatch:
    inputs:
      force_build:
        description: '强制重新构建 (即使镜像已存在)'
        required: false
        type: boolean
        default: false

jobs:
  check:
    runs-on: ubuntu-latest
    outputs:
      runners_version: ${{ steps.get_version.outputs.runners_version }}
      should_build: ${{ steps.check.outputs.should_build }}

    steps:
      - name: 拉取当前仓库代码
        uses: actions/checkout@v3

      - name: 设置 runners 版本
        id: get_version
        run: |
          # 固定版本
          RUNNERS_VERSION="2.0.3"
          echo "runners_version=$RUNNERS_VERSION" >> "$GITHUB_OUTPUT"
          echo "设置 runners 版本: ${RUNNERS_VERSION}"

      - name: 设置 Docker Buildx 用于多架构检查
        uses: docker/setup-buildx-action@v3

      - name: 检查 Docker Hub 上的多架构镜像是否存在
        id: check
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          RUNNERS_VERSION: ${{ steps.get_version.outputs.runners_version }}
        run: |
          SHOULD_BUILD="false"

          # 检查 runners 镜像
          RUNNERS_IMAGE="${DOCKERHUB_USERNAME}/n8n-runners"
          RUNNERS_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "https://hub.docker.com/v2/repositories/${RUNNERS_IMAGE}/tags/${RUNNERS_VERSION}")
          echo "检查 runners 镜像 ${RUNNERS_IMAGE}:${RUNNERS_VERSION} -> 状态码: ${RUNNERS_STATUS}"

          if [[ "${{ github.event.inputs.force_build }}" == "true" || "$RUNNERS_STATUS" != "200" ]]; then
            SHOULD_BUILD="true"
            echo "需要构建 runners 镜像: 强制构建=${{ github.event.inputs.force_build }}, 镜像不存在=$([[ "$RUNNERS_STATUS" != "200" ]] && echo true || echo false)"
          else
            # 如果镜像存在，进一步检查多架构支持
            echo "正在检查 runners 镜像的多架构支持..."
            docker buildx imagetools inspect "${RUNNERS_IMAGE}:${RUNNERS_VERSION}" 2>/dev/null && {
              HAS_AMD64=$(docker buildx imagetools inspect "${RUNNERS_IMAGE}:${RUNNERS_VERSION}" | grep -q "Platform:    linux/amd64" && echo "true" || echo "false")
              HAS_ARM64=$(docker buildx imagetools inspect "${RUNNERS_IMAGE}:${RUNNERS_VERSION}" | grep -q "Platform:    linux/arm64" && echo "true" || echo "false")
              echo "架构检查结果: AMD64=$HAS_AMD64, ARM64=$HAS_ARM64"
              if [[ "$HAS_AMD64" == "true" && "$HAS_ARM64" == "true" ]]; then
                echo "runners 镜像已支持 linux/amd64 和 linux/arm64 架构"
              else
                echo "runners 镜像架构不完整: AMD64=$HAS_AMD64, ARM64=$HAS_ARM64"
                SHOULD_BUILD="true"
              fi
            } || {
              echo "无法检查 runners 镜像架构，需要重新构建"
              SHOULD_BUILD="true"
            }
          fi

          echo "should_build=${SHOULD_BUILD}" >> "$GITHUB_OUTPUT"

  build-and-push:
    needs: check
    if: needs.check.outputs.should_build == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: 拉取代码
        uses: actions/checkout@v3

      - name: 设置 Docker Hub 用户名
        run: echo "DOCKERHUB_USERNAME=$(echo '${{ secrets.DOCKERHUB_USERNAME }}' | xargs)" >> "$GITHUB_ENV"

      - name: 登录到 Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 构建并推送 Task Runners 镜像
        uses: docker/build-push-action@v5
        with:
          context: ./runners
          file: ./runners/Dockerfile.runners
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ env.DOCKERHUB_USERNAME }}/n8n-runners:${{ needs.check.outputs.runners_version }}
            ${{ env.DOCKERHUB_USERNAME }}/n8n-runners:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          labels: |
            org.opencontainers.image.title=n8n Task Runners
            org.opencontainers.image.description=n8n task runners with Playwright support
            org.opencontainers.image.version=${{ needs.check.outputs.runners_version }}
            org.opencontainers.image.created=$(date -u +'%Y-%m-%dT%H:%M:%SZ')
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.licenses=MIT

      - name: 输出构建信息
        run: |
          echo "✅ Task Runners 镜像构建成功"
          echo "📦 镜像标签: ${{ env.DOCKERHUB_USERNAME }}/n8n-runners:${{ needs.check.outputs.runners_version }}"
          echo "🏗️  支持架构: linux/amd64, linux/arm64"
          echo "🔗 Docker Hub: https://hub.docker.com/r/${{ env.DOCKERHUB_USERNAME }}/n8n-runners"
          echo "🎭 包含 Playwright 1.56.0 支持"

  skip-build:
    needs: check
    if: needs.check.outputs.should_build == 'false'
    runs-on: ubuntu-latest
    steps:
      - name: 镜像已存在，跳过构建
        run: |
          echo "✅ 镜像已存在且支持多架构，跳过构建"
          echo "📦 镜像: ${{ secrets.DOCKERHUB_USERNAME }}/n8n-runners:${{ needs.check.outputs.runners_version }}"
          echo "🔗 Docker Hub: https://hub.docker.com/r/${{ secrets.DOCKERHUB_USERNAME }}/n8n-runners"